{{- if lookPath "atuin" -}}
{{ output "atuin" "init" "nu" "--disable-up-arrow" | replace "$cmd e>| complete" "$cmd | complete" | replace "job spawn -t atuin" "job spawn --description atuin" -}}
{{- end -}}
