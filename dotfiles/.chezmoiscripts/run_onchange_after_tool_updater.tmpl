{{- if eq .chezmoi.os "linux" -}}
#!/usr/bin/env sh
set -eu

if command -v systemctl >/dev/null 2>&1 && systemctl --user show-environment >/dev/null 2>&1; then
  systemctl --user daemon-reload
  systemctl --user enable --now nix-dotfiles-tool-update.timer
fi
{{- end -}}
