---
- name: Install Go from upstream tarball when missing
  ansible.builtin.script:
    cmd: install-go.sh "{{ dotfiles_cache_dir }}/go" "{{ dotfiles_bin_dir }}"
  args:
    creates: "{{ dotfiles_bin_dir }}/go"
  when: common_system in ['Darwin', 'Linux']
  tags:
    - bootstrap
    - go
